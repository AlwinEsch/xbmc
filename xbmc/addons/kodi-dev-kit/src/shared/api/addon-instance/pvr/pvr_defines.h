/*
 *  Copyright (C) 2005-2021 Team Kodi
 *  This file is part of Kodi - https://kodi.tv
 *
 *  SPDX-License-Identifier: GPL-2.0-or-later
 *  See LICENSES/README.md for more information.
 */

/* File autogenerated, see xbmc/addons/kodi-dev-kit/tools/code-generator/code_generator.py */

#pragma once

#include "../../../../../include/kodi/c-api/addon-instance/pvr/pvr_defines.h"
#include "../../../../../src/shared/api/addon-instance/pvr/pvr_defines.h"
#include "src/shared/SharedGroups.h"

#include <string.h>
#include <string>
#include <tuple>
#include <vector>

/* Code below becomes on auto generation reused, hand edited parts should included here */
/*---AUTO_GEN_PARSE<HAND_EDITED_FIELD_1>---*/
/*---AUTO_GEN_PARSE<HAND_EDITED_FIELD_1_END>---*/

namespace KODI
{
namespace ADDONS
{
namespace INTERFACE
{

struct IFC_PVR_NAMED_VALUE
{
  IFC_PVR_NAMED_VALUE() = default;
  IFC_PVR_NAMED_VALUE(const PVR_NAMED_VALUE* c_data)
  {
    if (c_data == nullptr)
      return;

    memcpy(strName.data(), c_data->strName, PVR_ADDON_NAME_STRING_LENGTH);
    memcpy(strValue.data(), c_data->strValue, PVR_ADDON_NAME_STRING_LENGTH);
  }

  void SetCStructure(PVR_NAMED_VALUE* c_data)
  {
    if (c_data == nullptr)
      return;

    memcpy(c_data->strName, strName.data(), PVR_ADDON_NAME_STRING_LENGTH);
    memcpy(c_data->strValue, strValue.data(), PVR_ADDON_NAME_STRING_LENGTH);
  }

  std::array<char, PVR_ADDON_NAME_STRING_LENGTH> strName;
  std::array<char, PVR_ADDON_NAME_STRING_LENGTH> strValue;

  MSGPACK_DEFINE(strName, strValue);
};

struct IFC_PVR_ATTRIBUTE_INT_VALUE
{
  IFC_PVR_ATTRIBUTE_INT_VALUE() = default;
  IFC_PVR_ATTRIBUTE_INT_VALUE(const PVR_ATTRIBUTE_INT_VALUE* c_data)
  {
    if (c_data == nullptr)
      return;

    iValue = c_data->iValue;
    memcpy(strDescription.data(), c_data->strDescription, PVR_ADDON_ATTRIBUTE_DESC_LENGTH);
  }

  void SetCStructure(PVR_ATTRIBUTE_INT_VALUE* c_data)
  {
    if (c_data == nullptr)
      return;

    c_data->iValue = iValue;
    memcpy(c_data->strDescription, strDescription.data(), PVR_ADDON_ATTRIBUTE_DESC_LENGTH);
  }

  int iValue;
  std::array<char, PVR_ADDON_ATTRIBUTE_DESC_LENGTH> strDescription;

  MSGPACK_DEFINE(iValue, strDescription);
};

/* Code below becomes on auto generation reused, hand edited parts should included here */
/*---AUTO_GEN_PARSE<HAND_EDITED_FIELD_2>---*/
/*---AUTO_GEN_PARSE<HAND_EDITED_FIELD_2_END>---*/

} /* namespace INTERFACE */
} /* namespace ADDONS */
} /* namespace KODI */
